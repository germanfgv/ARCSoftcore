#ifndef funal.h
#define funal.h

#include "Node.h"
#include "dictionary.h"

//Prototipos branches
int be(Node **node);
int bcs(Node **node);
int bneg(Node **node);
int bvs(Node **node);
int ba(Node **node);

//Prototipo set
int sethi(Node **node);

#endif
